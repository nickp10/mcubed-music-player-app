<Window x:Class="mCubed.mCubedError"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	AllowsTransparency="True" Background="Transparent" Height="Auto" Width="Auto"
	ShowInTaskbar="False" WindowStyle="None" WindowStartupLocation="CenterOwner"
	SizeToContent="WidthAndHeight">
	<Window.Resources>
		<ResourceDictionary Source="Resources/Resources.xaml"/>
	</Window.Resources>
	<Border DataContext="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}" Background="{DynamicResource MainBrush}" Padding="15">
		<Grid MaxWidth="400">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="10"/>
				<RowDefinition Height="Auto"/>
			</Grid.RowDefinitions>
			<TextBlock FontWeight="Bold" Text="{Binding Message}" TextWrapping="Wrap" />
			<Button Grid.Row="2" Content="Close" Padding="10,5" HorizontalAlignment="Center" Click="OnCloseClicked" />
		</Grid>
	</Border>
</Window>